# GUI框架（阶段 6 后 main.py 不再使用 Flet，仅悬浮球子进程用 PySide6）
PySide6>=6.6.0  # 桌面悬浮球独立进程

# 屏幕信息（用于窗口位置验证，支持多显示器）
screeninfo>=0.8.0

# 系统通知（可选）
plyer>=2.1.0  # 用于系统原生通知

# LLM API
openai>=1.0.0

# 图像处理
Pillow>=9.0.0

# 自动化操作
pyautogui>=0.9.54

# 配置管理
dotenv

# 数据库
sqlalchemy>=2.0.0

# 数据处理
pandas~=3.0.1
openpyxl>=3.0.0

# 文档解析
python-docx>=0.8.11
pdfplumber>=0.10.0

# 中文分词
jieba>=0.42.1

# 音频处理
sounddevice>=0.4.6
numpy>=1.26.0,<2  # onnxruntime 1.17.x / sherpa-onnx 1.10.x 基于 NumPy 1.x ABI，暂不兼容 NumPy 2
scipy>=1.9.0

# ASR/TTS
onnxruntime-gpu==1.17.1  # 支持 CUDA 12.x

# UI Automation (Windows桌面自动化)
uiautomation>=2.0.0
comtypes>=1.4.0
opencv-python>=4.8.0  # 模板匹配 (automation/template_matcher.py)

# Live2D 渲染
live2d-py>=0.7.0
PyOpenGL>=3.1.0

# sherpa-onnx[cuda] --extra-index-url https://k2-fsa.github.io/sherpa-onnx/
sherpa-onnx==1.10.46

# IPC 优化
msgpack>=1.0.7

# 后端服务层（Tauri 重构，见 .trae/documents/frontend-tauri-refactor.md）
fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.7
python-multipart>=0.0.9
websockets>=12.0