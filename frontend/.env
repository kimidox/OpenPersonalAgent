# 浏览器 dev 模式：用 vite proxy 转发 /api 与 /ws，无需 VITE_BACKEND_URL
# Tauri 打包后：由 Rust 注入（invoke('get_backend_url')），覆盖此值
# Tauri dev + BACKEND_EXTERNAL：Rust 也会注入，但 fallback 用此值
VITE_BACKEND_URL=http://127.0.0.1:8765
VITE_BACKEND_TOKEN=
